+ публикация;
+ truncate reST;
+ пофиксить UPDATE в event.list;
+ обновить валидатор на альбомы;
+ ограничение на файл аватара;
+ статьи:
  + admin роль;
  + публикация;
+ "пульс проекта":
  + список статей;
  + добавить теги в статьи;
+ отрефакторить урлы в редиректах;
+ sidebar and header;
- добавить Google Analitics;
- демо;
- объяснения к полям формы;
- добавить markitup;

==========================
- "Берегите природу" и "Не мусорите"
  - небольшой банер;
  - пояснительная статья;

==========================
- добавление пользователей, авторизация;
- регистрация;
- комментарии;
- модерация с комментариями;
- рефакторинг taconite интеграции;

===========???=============
- ID для каждой сущности отдельно;
- названия для конкретных схем (url, routes);
- персоны ко многим событиям, а не к одному;

